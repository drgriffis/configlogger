# configlogger
Python utility for logging experimental configurations, for replicability/reproducibility

## Installation

1. Clone the repository
2. Add `configlogger` to your Python path

For example, if I cloned into `~/Github/configlogger`, I would add the following in my `~/.bashrc`:

```
export PYTHONPATH=${HOME}/Github/configlogger:${PYTHONPATH}
```
